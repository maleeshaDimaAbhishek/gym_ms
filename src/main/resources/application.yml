spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gym_system_db?createDatabaseIfNotExist=true
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show_sql: true